// Placeholder generated file
import 'package:flutter/material.dart';

class ConversationScreen extends StatelessWidget {
 const ConversationScreen({super.key, required this.pulseName});
 final String pulseName;
 @override Widget build(BuildContext context){return Scaffold(appBar: AppBar(title: Text(pulseName)), body: const Center(child: Text('Conversation Screen v1')));} }
